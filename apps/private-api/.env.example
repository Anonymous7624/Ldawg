# Private API Environment Configuration

# Server Port
PORT=3001

# MongoDB Connection String
# Format: mongodb://username:password@host:port/database?authSource=authDatabase
MONGO_URI=mongodb://appuser:YOUR_PASSWORD_HERE@127.0.0.1:27017/privatechat?authSource=privatechat

# JWT Secret (generate a strong random secret)
# Example: openssl rand -hex 64
JWT_SECRET=your_jwt_secret_here_replace_with_secure_random_string

# JWT Expiration (e.g., 7d, 24h, 60m)
JWT_EXPIRES_IN=7d

# Node Environment
NODE_ENV=production

# CORS Allowed Origins (comma-separated)
CORS_ORIGINS=https://ldawg7624.com,https://www.ldawg7624.com
