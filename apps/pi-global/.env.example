# Kennedy Chat - Environment Configuration
# Copy this file to .env and configure as needed
# DO NOT commit .env to git (it's in .gitignore)

# Database path (absolute path required)
DB_PATH=/home/ldawg7624/chat-data/chat.db

# Upload directory (absolute path required)
UPLOAD_DIR=/home/ldawg7624/chat-data/uploads

# WebSocket server port
WS_PORT=8080

# Upload server port
UPLOAD_PORT=8082

# Upload base URL (used for file URLs)
UPLOAD_BASE_URL=https://upload.ldawg7624.com

# Maximum messages to keep in history
MAX_MESSAGES=600

# Private API URL for admin authentication
PRIVATE_API_URL=https://api.simplechatroom.com
